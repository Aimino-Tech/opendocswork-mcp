# Smithery configuration for opendocswork-mcp
# https://smithery.ai/docs/deploy

startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction:
    |-
      (config) => ({ command: 'npx', args: ['-y', '@aimino/opendocswork-mcp'] })
